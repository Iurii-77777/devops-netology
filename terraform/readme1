*.a
# Не обрабатывать файлы с расширением .а

for_moved/
# Игнорировать файлы в каталоге for_moved

docs/*.txt
# Игнорировать файлы в все .txt файлы в каталоге docs/ , но например нет docs/files/*.txt

!docs/test.txt
# Но отслеживать файл test.txt несмотря на то, что мы игнарируем все файлы *.txt в этом каталоге
